import { View, Text } from "react-native";
import React from "react";

const Details = () => {
  return (
    <View>
      <Text>ComponentsAndProps</Text>
      <Text>
        Learn how to create reusable components and pass data using props.
        Challenge: Create a UserCard component that takes name, age, and email
        as props and displays them in a card layout.
      </Text>
    </View>
  );
};

export default Details;
